#include <iostream>

using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.
    while (true)
    {
        int inputNum;

        cin >> inputNum;

        if (inputNum == 0)
        {
            break;
        }

        cout << inputNum << endl;
    }

    return 0;
}