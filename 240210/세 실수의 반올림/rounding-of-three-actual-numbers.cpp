#include <iostream>

using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.
    float a, b, c;

    cin >> a;
    cin >> b;
    cin >> c;

    cout << fixed;
    cout.precision(3);

    cout << a << endl << b << endl << c;

    return 0;
}